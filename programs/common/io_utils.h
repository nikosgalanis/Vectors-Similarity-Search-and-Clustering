#pragma once

#include <iostream>
#include <fstream>
#include <string>
#include <list> 
#include <vector> 

#include "../../utils/math_utils.h"

std::vector<std::vector<double>> parse_input(std::string filename);